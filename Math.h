#ifndef MATH_H
#define MATH_H

class Math {
public:
    int abs(int x);
    int multiply(int x, int y);
    int divide(int x, int y);
    int min(int x, int y);
    int max(int x, int y);
    int sqrt(int x);
};

#endif